# Permit only the active local user to open the kernel uinput node.
# This avoids root daemons, world-writable input injection, and broad input-group membership.
KERNEL=="uinput", SUBSYSTEM=="misc", TAG+="uaccess", OPTIONS+="static_node=uinput"
